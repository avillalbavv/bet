{
  "name": "noir-private-casino",
  "version": "2.0.0",
  "private": true,
  "description": "NOIR social casino demo with extensible game providers",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
